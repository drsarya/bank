
#include <cstdlib>
#include <math.h>
#include "Town.h"
#include <stdio.h>
 
  
/*
int main(int argc, char* argv[])
{
	Town* town = new Town();
	Bank* bank = new Bank("45", 0.01);
	town->addBank(bank);
 
	Client* cl = new Client("111", new BaseBill());
	Client* c2 = new Client("222", new BaseBill());
	Client* c3 = new Client("333", new LawBill());
 
	bank->addClient(cl);
	bank->addClient(c2);
	bank->addClient(c3);

	 
	Client** arr = bank->getClients();
 
	for (int i = 0; i < bank->getLengthArr(); i++) {
		 
		printf("%s\n", arr[i]->getName());
	}

	bank->deleteClient(c2);

	for (int i = 0; i < bank->getLengthArr(); i++) {

		printf("%s\n", arr[i]->getName());
	}
	system("PAUSE");
	return EXIT_SUCCESS;
} */